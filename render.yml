services:
  - type: web
    name: keycloak
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: KEYCLOAK_ADMIN
        value: admin
      - key: KEYCLOAK_ADMIN_PASSWORD
        value: admin
    ports:
      - 8082
